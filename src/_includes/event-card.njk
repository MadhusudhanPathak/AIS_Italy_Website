{%- macro event_card(title, date, emoji='🗓️', participants='', description='', link='#') -%}
<div class="card hover-lift">
  <div class="flex items-center space-x-3 mb-4">
    <span class="text-3xl">{{ emoji }}</span>
    <h3 class="mb-0">{{ title }}</h3>
  </div>
  {% if date %}<p class="text-sm font-semibold text-accent mb-4">📅 {{ date }}</p>{% endif %}
  {% if description %}<p>{{ description }}</p>{% endif %}
  <div class="mt-4 pt-4 border-t border-gray-100">
    {% if participants %}<p class="text-sm text-gray-600">👥 {{ participants }}</p>{% endif %}
    {% if link %}<a href="{{ link }}" class="text-accent font-semibold text-sm mt-2 inline-block">Learn more →</a>{% endif %}
  </div>
</div>
{%- endmacro -%}
